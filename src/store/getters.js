const getters = {
  sidebar: state => state.app.sidebar,
  size: state => state.app.size,
  device: state => state.app.device,
  dict: state => state.dict.dict,
  visitedViews: state => state.tagsView.visitedViews,
  cachedViews: state => state.tagsView.cachedViews,
  token: state => state.user.token,
  avatar: state => state.user.avatar,
  id: state => state.user.id,
  name: state => state.user.name,
  nickName: state => state.user.nickName,
  introduction: state => state.user.introduction,
  roles: state => state.user.roles,
  permissions: state => state.user.permissions,
  backendMode: state => state.user.backendMode,
  isAgent: state => state.user.isAgent,
  userSiteCode: state => state.user.siteCode,
  agentCode: state => state.user.agentCode,
  userName: state => state.user.userName,
  siteConfig: state => state.site && state.site.siteConfig,
  siteTitle: state => (state.site && state.site.siteConfig && state.site.siteConfig.title) || '',
  siteLogo: state => (state.site && state.site.siteConfig && state.site.siteConfig.logoUrl) || '',
  siteCode: state => (state.site && state.site.siteConfig && state.site.siteConfig.siteCode) || state.user.siteCode,
  isSiteDetected: state => state.site && state.site.isDetected,
  permission_routes: state => state.permission.routes,
  topbarRouters: state => state.permission.topbarRouters,
  defaultRoutes: state => state.permission.defaultRoutes,
  sidebarRouters: state => state.permission.sidebarRouters,
  homePath: state => state.permission.homePath,
  homeTitle: state => state.permission.homeTitle
}
export default getters
