# 测试环境菜单对齐设计

## 目标

以测试环境左侧菜单为准，先完成第一轮菜单收敛。

## 本轮范围

- 隐藏一级菜单“本周需求说明”。
- “站点管理”仅展示：开站申请、站点列表、站点场馆管理、站点素材管理。
- 隐藏页面只移除菜单入口，不删除页面组件。
- 权限树根据调整后的可见路由生成，不保留隐藏菜单的页面权限。

## 实现方式

在后端 Mock 路由转换完成后统一应用测试环境菜单白名单。后续继续对齐菜单时，只维护该映射，不修改压缩后的 Mock 路由源。

## 验证

- 静态检查确认菜单名称、数量及隐藏项。
- 检查权限树。
- 本地启动后在浏览器检查左侧菜单。
